import React from 'react';
import styles from './Plug.module.css';

const BoardPlug = () => {
    return (
        <h2 className={styles.boardWarning}>
            There are not any lists. Create a couple!
        </h2>
    );
};

export default BoardPlug;