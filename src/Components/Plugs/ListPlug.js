import React from 'react';
import styles from './Plug.module.css';

const ListPlug = () => {
    return (
        <div className={styles.listWarning}>
            The list is empty. Add some tasks!
        </div>
    );
};

export default ListPlug;